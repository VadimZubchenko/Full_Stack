## About App works.

App creats database at run-time, so it's data don't save permanently.
User enters data using a form
After submiting data will be added to a table.
The table is present on the same page under the form.
User can remove or edit data in database using button Remove and Edit right side of table lines.
The table data is updated after deletion or editing.
For UI example see user_inerface.png.

#Show id in a table on web-side
Uncomment lines below to present 'id' in table on web

<!--
230 let clientId = document.createElement("th");
231 let clientIdtext = document.createTextNode(" id ");
232 clientId.appendChild(clientIdtext);-->

<!-- 259 headerRow.appendChild(clientId); -->

Comment line below to present 'id' in table on web
278 if (x === "id")
279 continue;

06.2023.
